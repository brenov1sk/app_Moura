<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appmoura">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".total"></activity>
        <activity android:name=".fimTurno" />
        <activity android:name=".hora7" />
        <activity android:name=".hora6" />
        <activity android:name=".hora5" />
        <activity android:name=".hora4" />
        <activity android:name=".hora3" />
        <activity android:name=".hora2" />
        <activity android:name=".hora1" />
        <activity android:name=".inicioTurno" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>